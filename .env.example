# SMTP credentials for the contact form (Next.js API route + Nodemailer)
# TODO: Configure with client's actual email provider + credentials
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-smtp-username
SMTP_PASS=your-smtp-password

# Address inquiries are sent to / from
# TODO: Replace with client's actual inbox
CONTACT_TO_EMAIL=hello@wecan.bg
CONTACT_FROM_EMAIL=website@wecan.bg
